package com.code.interviews.mx;

public class MXInterview {

    public static void main(String[] args) {
        int[] input = {5, 10,2,3,15,4,1};
      //  System.out.println(Arrays.toString(compute(input)));
    }

    /*public static int[] compute(int[] input) {
        int left = 0;
        int right = input.length - 1;
        int pivot = input.length / 2;

        while(input[left] > input[left+1])

            int leftTemp = input[left];
            input[left] = input[left+1];
            input[left+1] = leftTemp;

            int rightTemp = input


        }


            if(input[i] % 5 == 0) {
                input[i] = input[i] * 5;
            }
        }

        return input;
    }*/


 }
